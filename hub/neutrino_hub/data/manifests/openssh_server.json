{
  "_comment": "The SSH server. Every platform carries one, so nothing is downloaded and the drawer words the row enable/disable rather than install/uninstall; disabling is allowed because the agent channel, not SSH, is the management path.",
  "name": "openssh_server",
  "title": "SSH server",
  "kind": "openssh",
  "description": "Lets the gateway manage this machine directly over SSH",
  "platforms": {
    "linux-debian": {
      "packages": [
        "openssh-server"
      ],
      "service": "ssh"
    },
    "linux-rhel": {
      "packages": [
        "openssh-server"
      ],
      "service": "sshd"
    },
    "windows": {},
    "darwin": {}
  },
  "is_builtin": true
}
