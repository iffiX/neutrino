# images

Source artwork and the screenshots the README will use. Nothing here ships in
a package.

`icon.png` is the mark on a transparent background; the rendered application
icons come from it and live in `resources/`. `readme_guideline.md` is the plan
for the project README, which is not written yet.
