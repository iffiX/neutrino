[pytest]
# The tests here import each other's helpers by plain name, and the machine
# under test has no editable install of this directory.
pythonpath = .
# File order is the run order: the network tests walk a box from one shape to
# the next, and each starts from where the last left it.
addopts = -p no:randomly
